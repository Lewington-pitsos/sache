- see if we actually need sae_lens for the generator
- shuffler add some kin
- fix cache (at the moment all indices are borrowed and none returned when we iterate, we need to design an iterator where the cache manages its own loans)
- 